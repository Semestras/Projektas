<link rel="stylesheet" href="{{ asset('css/todo.css') }}"/>
<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css" rel="stylesheet">
<html>
<head>
    <title>This is your homepage</title>
</head>
<body>
<form action="/create">
    <input type="submit" value="Create a new task"/>
</form>

<form action="/DoneList">
    <input type="submit" value="See done list"/>
</form>

<form action="/logout">
    <input type="submit" value="Log me out"/>
</form>

<h1>All your available tasks:</h1>

<div class="container bootstrap snippet">
    <div class="row">
        <ul class="notes">
            {% for item in result %}
                <li>

                    <span class="text">Name : {{ item.name }}</span><br>
                    <span class="text">Description : {{ item.description }}</span><br>
                    <span class="deadln">Deadline : {{ item.duedate }}</span><br>

                    <button id="checkAll"><i class="fa fa-exclamation"
                                                                        style="font-size:16px"></i> Late
                        </button>
                    <button id="checkAll"><i class="fa fa-check" style="font-size:16px"></i>
                            DONE
                        </button>
                    <button id="checkAll"><i class="fa fa-check" style="font-size:16px"></i>
                        Delete
                    </button>
                    <br>

                </li><br>

            {% else %}
                <h3> It seems you have no active tasks, create one now :)</h3>
            {% endfor %}
        </ul>
    </div>
</div>
</body>
</html>

