<link rel="stylesheet" href="{{ asset('css/todo.css') }}"/>
<link rel="stylesheet" href="{{ asset('css/todo.css') }}"/>
<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css" rel="stylesheet">
<html>
<head>
    <title>This is your homepage</title>
</head>
<body>
<div class="background"></div>
<form action="/create">
    <input class="button" type="submit" value="Create a new task"/>
</form>
<form action="/DoneList">
    <input class="button" type="submit" value="See done list"/>
</form>
<form action="/logout">
    <input class="button" type="submit" value="Log me out"/>
</form>
<div class="allChart" style="float:right; width:50%;">
    <div class="latetitle">
        <h3 class="latetitlename">You are currently late for these tasks:</h3>
    </div>
    <div class="row">
        <ul class="notes">
            {% for item in result2 %}
                <li>
                    <span class="text">Name : {{ item.name }}</span><br>
                    <span class="desc">Description : {{ item.description }}</span><br>
                    <span class="deadln">Deadline : {{ item.duedate }}</span><br>
                    <button class="checkAll"><i class="fa fa-check" style="font-size:16px"></i>
                        DONE
                    </button>
                </li><br>
            {% else %}
                <h3 style="background: ghostwhite"> Congratulations, you are not late for any tasks :)</h3>
            {% endfor %}
        </ul>
    </div>
</div>
<div class="container bootstrap snippet">
    <div style="float:left; width:50%;">
            <div class="box box-aqua">
                <div class="box-header ui-sortable-handle" style="cursor: move;">
                    <i class="ion ion-clipboard"></i>
                    <div class="title">
                        <h2 class="titlestyle">All your available tasks:</h2>
                    </div>
                </div>
                <div class="box-body">
                    <ul class="todo-list ui-sortable">
                        {% for item in result1 %}
                            <li>
                                <span class="text">Name : {{ item.name }}</span><br>
                                <span class="desc">Description : {{ item.description }}</span><br>
                                <span class="deadln">Deadline : {{ item.duedate }}</span><br>
                                <button class="checkAll"><i class="fa fa-check" style="font-size:16px"></i>
                                    DONE
                                </button>
                                <br>
                            </li><br>
                        {% else %}
                            <h3 style="background: ghostwhite"> It seems you have no active tasks, create one now
                                :)</h3>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>

</div>
</body>
</html>