<link rel="stylesheet" href="{{ asset('css/todo.css') }}"/>
<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css" rel="stylesheet">
<head>
    <title>Completed tasks</title>
</head>
<body>
<h3>All your completed tasks:</h3>
<div class="container bootstrap snippet">
    <div class="row">
        <ul class="notes">
            {% for item in result %}
                <li>
                    <span class="text">Name : {{ item.name }}</span><br>
                    <span class="text">Description : {{ item.description }}</span><br>
                    <span class="deadln">Deadline : {{ item.duedate }}</span><br>
                </li><br>
            {% else %}
                <h3> It seems you have no completed tasks, maybe you should start working now? :)</h3>
            {% endfor %}
        </ul>
    </div>
</div>